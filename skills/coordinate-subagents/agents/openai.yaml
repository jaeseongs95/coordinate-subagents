interface:
  display_name: "Coordinate Subagents"
  short_description: "Delegate only when coordination has a documented net benefit"
  default_prompt: "Use $coordinate-subagents to justify useful delegation, assign it safely, and integrate verified results."

policy:
  allow_implicit_invocation: true
