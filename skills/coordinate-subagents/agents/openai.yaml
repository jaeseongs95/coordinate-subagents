interface:
  display_name: "Coordinate Subagents"
  short_description: "Delegate independent work and coordinate verified results"
  default_prompt: "Use $coordinate-subagents to delegate independent work safely and integrate verified results."

policy:
  allow_implicit_invocation: true
